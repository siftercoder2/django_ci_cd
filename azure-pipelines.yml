# Docker
- task: Docker@2
  inputs:
    containerRegistry: 'djangocicd2 service connection'
    repository: 'djangocicd2'
    command: 'buildAndPush'
    Dockerfile: '**/dockerfile'
- task: Docker@2
  inputs:
    containerRegistry: 'djangocicd2 service connection'
    repository: 'djangocicd2'
    command: 'buildAndPush'
    Dockerfile: '**/dockerfile'
# Build a Docker image 
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:  
  - job: Build
    displayName: Build
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - task: Docker@2
      displayName: Build an image
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: '**/dockerfile'
        tags: |
          $(tag)